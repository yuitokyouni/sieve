{
  "fixture_id": "canary/semantic-lob-min",
  "fixture_version": "1.0.0",
  "mode": "semantic",
  "status": "ready",
  "summary": "Cross-engine behavioural check. min-lob-b is an independent implementation of the same market as the reference min-lob-a: different data structures, opposite fill-leg order, different snapshot order, its own ext.* keys. Its event log is deliberately NOT byte-identical to the reference, so the exact canary rejects it and the semantic canary must accept it. A semantic canary that started rejecting min-lob-b would have begun depending on representation.",
  "not_derived_from": "tests/golden/fixtures/ is NOT reused. See exact-lob-min/fixture.json for the reason.",
  "subject": {
    "engine_id": "min-lob-b",
    "engine_version": "1.0.0",
    "entrypoint": "_engine.min_lob_b:MinLobB",
    "config_path": "config.json",
    "stats_variance_method": "naive",
    "rng_algorithm": "splitmix64",
    "rng_version": "1"
  },
  "reference": {
    "engine_id": "min-lob-a",
    "engine_version": "1.0.0",
    "entrypoint": "_engine.min_lob_a:MinLobA",
    "config_path": "config.json",
    "stats_variance_method": "two_pass"
  },
  "precondition": {
    "kind": "common_surface_comparison",
    "statement": "The two engines are DIFFERENT engines, so behavior_config_hash agreement is not available and would be meaningless: the hash covers a configuration domain, not two independent codebases. The precondition is therefore the field-by-field common-surface comparison table (ext.* excluded), emitted at run time and referenced from the CanaryResult by digest so the assertions cannot drift away from the table they rest on.",
    "table_path": "common_surface_comparison.json",
    "same_engine_variant": "If this fixture is ever reused for the same-engine / different-environment case, the precondition kind becomes behavior_config_hash_match and the table is not produced. The two cases are different preconditions for the same assertions, which is why CanaryResult models precondition_evidence as a oneOf rather than a boolean."
  },
  "assertion": {
    "statement": "Five families, all computable from the common 8 fields alone. Adoption and non-adoption are both recorded.",
    "adopted": [
      {
        "assertion_id": "semantic.conservation.buy",
        "kind": "conservation",
        "statement": "submitted_quantity_buy == filled_quantity_buy + cancelled_quantity_buy + expired_quantity_buy + terminal_resting_quantity_buy",
        "why_closable": "The terminal book_level events carry resting depth using only the common 8 fields, so the identity closes without a new record type and without ext.*. Without a terminal state record the identity does not close at all — recorded as gap G5."
      },
      {
        "assertion_id": "semantic.conservation.sell",
        "kind": "conservation",
        "statement": "The same identity on the sell side."
      },
      {
        "assertion_id": "semantic.two_sided_equality",
        "kind": "two_sided_equality",
        "statement": "For every t, the buy-leg quantity summed over order_fill events equals the sell-leg quantity. Checked per t rather than only in total, so two offsetting errors in different steps cannot cancel."
      },
      {
        "assertion_id": "semantic.no_crossing",
        "kind": "no_crossing",
        "statement": "In the terminal snapshot, max(buy price) < min(sell price).",
        "scope_limit": "SNAPSHOT-TIME ONLY, not per event. The per-event form needs Level-I state at every event, which is exactly gap G1; asserting it per event today would silently freeze the provisional `l1` field into the contract. The restriction is the honest version of this assertion until G1 is closed."
      },
      {
        "assertion_id": "semantic.sign_domain",
        "kind": "sign_domain",
        "statement": "Every order_fill / order_cancel / order_expire / book_level event has price > 0 and quantity > 0, and side in {buy, sell}. Direction is carried by `side` alone; a signed quantity would make every conservation identity depend on a sign convention that is not in the contract."
      },
      {
        "assertion_id": "semantic.event_count",
        "kind": "event_count",
        "statement": "Total event count and per-type counts equal the reference. Exact, tolerance zero: the two engines emit the same events in a different order, not a different number of them."
      },
      {
        "assertion_id": "semantic.stats_vector",
        "kind": "statistic_tolerance",
        "statement": "Every stats_vector element lands within its declared tolerance of the reference, element by element (not as a norm, which would let one large deviation hide inside many small ones)."
      }
    ],
    "not_adopted": [
      {
        "candidate": "per-event no-crossing",
        "reason": "Requires Level-I at every event (gap G1); adopting it now would freeze the provisional `l1` field before 2026-08-22 decides how L1 is obtained."
      },
      {
        "candidate": "per-order quantity conservation",
        "reason": "Requires `order_id` on the common surface (gap G5); the aggregate per-side identity is adopted instead and closes with the common 8 fields."
      },
      {
        "candidate": "order-flow-imbalance agreement between engines",
        "reason": "OFI is defined on consecutive Level-I states and inherits G1; it belongs to the Cont harness (docs/contract/cont_analysis_io.md), not to a fixture that must stay runnable before G1 is closed."
      }
    ]
  },
  "tolerance": {
    "statement": "Every tolerance names its basis. `structural` means exact by construction; `numerical` means floating-point or summation-order error with a stated magnitude; `seed_variation` means the spread across seeds of one configuration. No tolerance here is `provisional` — a provisional tolerance would have to be labelled as such and could not carry a claim.",
    "entries": [
      {
        "applies_to": "every integer-valued stats_vector element (indices 0-19)",
        "value": 0,
        "kind": "absolute",
        "basis": "structural",
        "basis_note": "Counts and lot quantities are integers produced by identical integer arithmetic in both engines. Any difference is a behaviour difference, not noise."
      },
      {
        "applies_to": "trade_price_mean (index 20)",
        "value": 1e-9,
        "kind": "absolute",
        "basis": "numerical",
        "basis_note": "Both engines average the same integer-valued prices, so the difference is zero in practice; the tolerance is kept non-zero only to avoid asserting bit-equality of a float division. Observed difference at fixture minting: 0."
      },
      {
        "applies_to": "trade_price_variance (index 21)",
        "value": 1e-8,
        "kind": "absolute",
        "basis": "numerical",
        "basis_note": "min-lob-a computes the sample variance in two passes; min-lob-b from the sum of squares. On prices near 1000 the sum-of-squares form loses accuracy to cancellation: sum(x^2) ~ 4e7 against (sum x)^2/n ~ 4e7 for a difference of order 1e1, giving a relative error of order 1e-16 * 4e7 / 1e1 ~ 4e-10 in absolute terms. Observed difference at fixture minting: 3.82e-11 tick^2. The tolerance is set two orders of magnitude above the observed value and below any difference that would indicate a changed statistic."
      }
    ],
    "seed_variation_note": "No tolerance here is derived from seed variation, because both engines run the SAME seed and the same stream. A seed-variation tolerance would be required if the semantic canary ever compared different seeds, and would have to be estimated from a declared number of seeds with the +/- convention stated (SD or SE, ddof, n)."
  },
  "generation": {
    "command": "python3 fixtures/canary/run_canary.py --fixture semantic-lob-min --mint",
    "working_directory": "repository root",
    "environment": "CPython >= 3.11, standard library only.",
    "expected_path": "expected.json"
  }
}

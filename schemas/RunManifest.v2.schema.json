{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/yuitokyouni/sieve/schemas/RunManifest.v2.schema.json",
  "title": "RunManifest",
  "description": "Run manifest, Evidence Contract v0.1 (schema_version 2.0.0). Hand-authored contract artifact; NOT generated by `sieve schemas export`. v1 (schemas/RunManifest.schema.json, schema_version 0.1.0) is retained unchanged and is DEPRECATED for new runs: it is kept only so that already-sealed v1 bundles keep validating. New runs MUST emit v2. Normative prose: docs/contract/evidence_contract_v0.1.md.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "run_id",
    "created_at",
    "engine",
    "producer",
    "command",
    "master_seed",
    "seed_convention_version",
    "rng_algorithm",
    "rng_version",
    "runtime_fingerprint_domain_version",
    "environment",
    "input_artifact_digests",
    "effective_config"
  ],
  "properties": {
    "schema_version": {
      "description": "Contract version of this manifest. Bumped on any change to the required set or to a hash domain.",
      "const": "2.0.0"
    },
    "run_id": {
      "description": "Volatile per-run identifier. Excluded from the scientific seal.",
      "type": "string",
      "minLength": 1
    },
    "created_at": {
      "description": "Volatile. RFC 3339, UTC, second precision or finer. Excluded from the scientific seal.",
      "type": "string",
      "format": "date-time"
    },
    "engine": {
      "description": "Calendar 2.1 line 1. The ENGINE that produced the run — distinct from `producer`, which is the tool that wrote this manifest. Conflating them is how a run gets attributed to the harness version instead of the model version.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "engine_id",
        "engine_version"
      ],
      "properties": {
        "engine_id": {
          "type": "string",
          "minLength": 1
        },
        "engine_version": {
          "type": "string",
          "minLength": 1
        },
        "git_commit": {
          "type": "string",
          "pattern": "^[0-9a-f]{7,40}$"
        },
        "source_digest": {
          "$ref": "#/$defs/Sha256Hex",
          "description": "Digest of the engine source tree as run. Distinct from git_commit: a dirty tree has a commit and is not that commit."
        }
      }
    },
    "producer": {
      "description": "What wrote this manifest.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "tool_id",
        "tool_version"
      ],
      "properties": {
        "tool_id": {
          "type": "string",
          "minLength": 1
        },
        "tool_version": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "command": {
      "description": "Volatile (machine-local). Full argv as invoked, including every override flag. Excluded from the scientific seal but REQUIRED to be present: BACKLOG 'Evidence Contract v0.1' item 3 (CLI override paths must be recoverable).",
      "type": "string"
    },
    "event_log_schema_version": {
      "description": "Calendar 2.1 line 7. Version of the EventLog schema the run's event log conforms to. Recorded in the manifest, not only inside the log, so a manifest can be checked without opening the log.",
      "type": "string",
      "minLength": 1
    },
    "metric_suite": {
      "description": "Calendar 2.1 line 8.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "suite_id",
        "suite_version"
      ],
      "properties": {
        "suite_id": {
          "type": "string",
          "minLength": 1
        },
        "suite_version": {
          "type": "string",
          "minLength": 1
        },
        "suite_hash": {
          "$ref": "#/$defs/Sha256Hex"
        },
        "output_digest": {
          "$ref": "#/$defs/Sha256Hex",
          "description": "Contract digest of the suite's output, canonical form 'output_table'."
        }
      }
    },
    "code_provenance": {
      "description": "Q1: OPTIONAL at the schema layer. Absence is a WARN surfaced by the reader, never a schema error. FAIL enforcement, where a profile requires it, is done by the conformance profile layer (docs/contract/evidence_contract_v0.1.md §6), not here.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "git_commit": {
          "type": "string",
          "pattern": "^[0-9a-f]{7,40}$"
        },
        "git_dirty": {
          "type": "boolean"
        },
        "container_digest": {
          "type": "string",
          "pattern": "^sha256:[0-9a-f]{64}$"
        }
      }
    },
    "master_seed": {
      "type": "integer"
    },
    "seed_convention_version": {
      "description": "B8. Version of the seed-derivation convention (master SeedSequence -> named children, spawn order). Included in the behavior_config_hash domain: changing the convention changes behaviour even at a fixed master_seed, so it must move the hash.",
      "type": "string",
      "minLength": 1
    },
    "seed_tree": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "name",
          "entropy"
        ],
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1
          },
          "entropy": {
            "type": "integer"
          },
          "spawn_key": {
            "type": "array",
            "items": {
              "type": "integer"
            }
          }
        }
      }
    },
    "rng_algorithm": {
      "description": "Q2: first-class typed top-level field, NOT an environment map key. Generator family actually used (e.g. 'numpy.PCG64', 'splitmix64').",
      "type": "string",
      "minLength": 1
    },
    "rng_version": {
      "description": "Q2: first-class typed top-level field. Version of that generator's stream definition, so a stream change is visible without diffing implementations.",
      "type": "string",
      "minLength": 1
    },
    "runtime_fingerprint_domain_version": {
      "description": "Q2: integer version of the SET OF KEYS hashed into environment_fingerprint_digest. The key list is registered in docs/contract/effective_config.md (registry section). Adding a key to `environment` does NOT change the digest; changing the registered domain REQUIRES bumping this integer, which makes the change visible in every downstream digest.",
      "type": "integer",
      "minimum": 1
    },
    "environment": {
      "$ref": "#/$defs/EnvironmentFingerprint"
    },
    "input_artifact_digests": {
      "description": "Every declared input, content-addressed. TYPED MAP (the A/B compromise approved 2026-08-20): the key is the artifact id, so an id cannot be duplicated and a lookup needs no scan; the value is a fully typed record, so 'which kind of input is this and where did it come from' is not lost the way a flat id->digest map loses it. Calendar 2.1 line 4. Backlog 'Evidence Contract v0.1' item 1: calibration constants are inputs with a source reference and a digest, not code defaults.",
      "type": "object",
      "propertyNames": {
        "pattern": "^[A-Za-z0-9][A-Za-z0-9._/-]*$",
        "minLength": 1
      },
      "additionalProperties": {
        "$ref": "#/$defs/InputArtifactDigest"
      }
    },
    "effective_config": {
      "$ref": "#/$defs/EffectiveConfigRef"
    },
    "conformance_profile_id": {
      "description": "DECLARATION ONLY: which conformance profile this run claims to satisfy. The manifest does NOT carry per-item pass/fail. A manifest that judged itself would be a second authority on conformance, and the one that is easiest to make agree with itself. The static item -> field_pointer table is docs/contract/conformance_map.v1.json; per-run status is reported by the profile checker, beside the manifest, not inside it.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "profile_id",
        "profile_version"
      ],
      "properties": {
        "profile_id": {
          "type": "string",
          "minLength": 1
        },
        "profile_version": {
          "type": "string",
          "minLength": 1
        },
        "map_ref": {
          "type": "string",
          "description": "Path of the static item -> field pointer table this profile is defined against."
        },
        "map_digest": {
          "$ref": "#/$defs/Sha256Hex"
        }
      }
    },
    "canary_results": {
      "description": "References to canary evaluations attached to this run. REFERENCE + DIGEST ONLY: no CanaryResult payload may be duplicated here (enforced structurally by additionalProperties:false and by the absence of any payload-shaped property). Placement rationale in docs/contract/evidence_contract_v0.1.md §5.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/CanaryResultRef"
      }
    },
    "outputs": {
      "description": "Content-addressed outputs of this run, in the canonical forms defined by docs/contract/canonicalization.md.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/OutputArtifactDigest"
      }
    },
    "notes": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "$defs": {
    "Sha256Hex": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "Lowercase hex sha256. sha256 is fixed by the contract; there is no algorithm-agility field anywhere in v2."
    },
    "EnvironmentFingerprint": {
      "description": "Q2, reserved-key scheme. A free string->string map whose RESERVED keys are named below for typing and documentation. Only the keys registered in docs/contract/effective_config.md (registry section) enter environment_fingerprint_digest; unregistered keys are recorded but not hashed. dependency_lock_digest is a RESERVED KEY here, not a top-level field.",
      "type": "object",
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9_.]*$"
      },
      "additionalProperties": {
        "type": "string"
      },
      "properties": {
        "python": {
          "type": "string",
          "description": "Interpreter version, e.g. '3.11.15'. Hashed."
        },
        "platform": {
          "type": "string",
          "description": "platform.platform(). Hashed."
        },
        "numpy": {
          "type": "string",
          "description": "numpy version. Hashed."
        },
        "blas": {
          "type": "string",
          "description": "BLAS/LAPACK implementation identity, e.g. 'openblas 0.3.27'. Hashed."
        },
        "dependency_lock_digest": {
          "$ref": "#/$defs/Sha256Hex",
          "description": "sha256 of the canonicalized dependency lock file. Reserved key, hashed."
        }
      }
    },
    "InputArtifactDigest": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "artifact_type",
        "digest_algorithm",
        "verification"
      ],
      "properties": {
        "artifact_type": {
          "enum": [
            "dataset",
            "calibration",
            "config",
            "code",
            "reference",
            "other"
          ]
        },
        "digest": {
          "$ref": "#/$defs/Sha256Hex",
          "description": "Absent iff verification.status == 'unverifiable'."
        },
        "digest_algorithm": {
          "const": "sha256"
        },
        "source_reference": {
          "description": "Where the artifact came from, outside this repository's code. REQUIRED when artifact_type == 'calibration' (see the if/then below).",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind",
            "locator"
          ],
          "properties": {
            "kind": {
              "enum": [
                "uri",
                "doi",
                "measurement",
                "vendor",
                "derived",
                "manual"
              ]
            },
            "locator": {
              "type": "string",
              "minLength": 1
            },
            "retrieved_at": {
              "type": "string",
              "format": "date-time"
            },
            "evidence_basis": {
              "description": "How the value's provenance is established. 'external_measurement_not_rerunnable' is the case that motivated this field (BACKLOG, phi/sigma).",
              "enum": [
                "primary_artifact",
                "external_measurement_rerunnable",
                "external_measurement_not_rerunnable",
                "literature",
                "unrecorded"
              ]
            }
          }
        },
        "role": {
          "type": "string",
          "description": "Free-text role of this input in the run."
        },
        "verification": {
          "description": "B5, AUDIT-TIME CLASSIFICATION. Separate clause from the run-time measure (fail-fast). 'unverifiable' means: the input was declared, but no digest could be obtained for it at consumption time.",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "status"
          ],
          "properties": {
            "status": {
              "enum": [
                "verified",
                "unverifiable"
              ]
            },
            "reason": {
              "description": "Required when status == 'unverifiable'.",
              "type": "string",
              "minLength": 1
            }
          },
          "if": {
            "properties": {
              "status": {
                "const": "unverifiable"
              }
            }
          },
          "then": {
            "required": [
              "status",
              "reason"
            ]
          }
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "artifact_type": {
                "const": "calibration"
              }
            },
            "required": [
              "artifact_type"
            ]
          },
          "then": {
            "required": [
              "source_reference"
            ]
          }
        },
        {
          "if": {
            "properties": {
              "verification": {
                "properties": {
                  "status": {
                    "const": "verified"
                  }
                },
                "required": [
                  "status"
                ]
              }
            },
            "required": [
              "verification"
            ]
          },
          "then": {
            "required": [
              "digest"
            ]
          }
        }
      ],
      "description": "One declared input. The artifact id is the MAP KEY, not a field: carrying it in both places would allow them to disagree."
    },
    "EffectiveConfigRef": {
      "description": "The resolved configuration actually in force, after defaults, files and CLI overrides. BACKLOG 'Evidence Contract v0.1' item 3.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "effective_config_digest",
        "behavior_config_hash",
        "behavior_config_domain_version",
        "registry_ref"
      ],
      "properties": {
        "effective_config_digest": {
          "$ref": "#/$defs/Sha256Hex",
          "description": "sha256 of the canonicalized full effective config (docs/contract/canonicalization.md 3.1)."
        },
        "behavior_config_hash": {
          "$ref": "#/$defs/Sha256Hex",
          "description": "sha256 over the behaviour-determining subset only (config keys + seed_convention_version + rng_algorithm + rng_version). Two runs with equal behavior_config_hash are expected to be behaviourally identical; this is the precondition of a same-engine semantic canary."
        },
        "behavior_config_domain_version": {
          "type": "integer",
          "minimum": 1,
          "description": "Version of the key set entering behavior_config_hash. Registered in docs/contract/effective_config.md."
        },
        "environment_fingerprint_digest": {
          "$ref": "#/$defs/Sha256Hex",
          "description": "sha256 over the REGISTERED environment keys only, at runtime_fingerprint_domain_version."
        },
        "registry_ref": {
          "description": "Pointer to the registry document and its version that defines the two hash domains and the formula registry.",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "document",
            "version"
          ],
          "properties": {
            "document": {
              "type": "string",
              "minLength": 1
            },
            "version": {
              "type": "string",
              "minLength": 1
            }
          }
        },
        "formula_bindings": {
          "description": "Which formula id@version was in force for each formula slot, and the effective values it resolved to. Mirrors the formula registry section of effective_config.md.",
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "formula_id",
              "formula_version"
            ],
            "properties": {
              "formula_id": {
                "type": "string",
                "minLength": 1
              },
              "formula_version": {
                "type": "string",
                "minLength": 1
              },
              "effective_values": {
                "type": "object"
              }
            }
          }
        },
        "resolution_sources": {
          "description": "Calendar 2.1 line 5: what was resolved INTO the effective config — CLI flags, config files, environment variables, code defaults. The resolved result is `effective_config_digest`; this is the audit trail of how it got that way. Backlog 'Evidence Contract v0.1' item 3: without it, an override path produces a run that is off-spec with nothing recording it.",
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "source_kind"
            ],
            "properties": {
              "source_kind": {
                "enum": [
                  "cli",
                  "config_file",
                  "environment_variable",
                  "default"
                ]
              },
              "ref": {
                "type": "string",
                "description": "Flag name, file path, variable name, or the code location the default came from."
              },
              "digest": {
                "$ref": "#/$defs/Sha256Hex",
                "description": "For config_file sources."
              },
              "keys_set": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Which effective-config keys this source決定した. Makes 'who won' recoverable when two sources touch one key."
              }
            }
          }
        }
      }
    },
    "CanaryResultRef": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "fixture_id",
        "fixture_version",
        "mode",
        "result_digest"
      ],
      "properties": {
        "fixture_id": {
          "type": "string",
          "minLength": 1
        },
        "fixture_version": {
          "type": "string",
          "minLength": 1
        },
        "fixture_digest": {
          "$ref": "#/$defs/Sha256Hex"
        },
        "mode": {
          "enum": [
            "exact",
            "semantic"
          ]
        },
        "result_digest": {
          "$ref": "#/$defs/Sha256Hex",
          "description": "sha256 of the canonicalized CanaryResult document."
        },
        "result_ref": {
          "type": "string",
          "description": "Path or URI of the CanaryResult document, relative to the run directory when relative."
        },
        "verdict": {
          "description": "Copy of the referenced result's verdict, for reading without dereferencing. Classification only.",
          "enum": [
            "MATCH",
            "MISMATCH",
            "UNVERIFIABLE",
            "PENDING_GENERATION"
          ]
        }
      }
    },
    "OutputArtifactDigest": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "artifact_id",
        "canonical_form",
        "digest",
        "digest_algorithm"
      ],
      "properties": {
        "artifact_id": {
          "type": "string",
          "minLength": 1
        },
        "canonical_form": {
          "description": "Which canonicalization rule produced the bytes that were hashed (docs/contract/canonicalization.md).",
          "enum": [
            "effective_config",
            "event_log",
            "stats_vector",
            "output_table",
            "bundle"
          ]
        },
        "digest": {
          "$ref": "#/$defs/Sha256Hex",
          "description": "Contract digest: sha256 over the canonical serialization. Chain-bearing."
        },
        "digest_algorithm": {
          "const": "sha256"
        },
        "path": {
          "type": "string"
        },
        "byte_digest": {
          "$ref": "#/$defs/Sha256Hex",
          "description": "sha256 of the stored file's bytes. Artifact identity only; never used by the hash chain."
        }
      },
      "description": "One content-addressed output. `digest` is the CONTRACT digest, computed over the canonical serialization named by `canonical_form` — that is the digest the 2026-08-24 hash chain links. `byte_digest` is a different quantity with a different purpose: the sha256 of the file as stored, for artifact identity and transfer integrity. They are given separate names because two writers can agree on the contract digest and disagree on the bytes, and reporting one under the other's name is how that difference gets mistaken for a scientific one (gap G6)."
    }
  }
}

"""Diagnostic figures: versioned, deterministic, dependency-free SVG.

Figures are evidence artifacts, sealed like every other artifact. They are
rendered as static SVG with no external fonts, scripts or network access, so
a report directory stays readable anywhere, forever.
"""

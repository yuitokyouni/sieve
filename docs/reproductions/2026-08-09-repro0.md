# Reproduction #0 — fresh clone, fresh venv, pinned environment

Self-reproduction performed before any external reproduction request, so
that asking a third party is a request to check a published, already-executed
contract rather than a first trial. Recorded verbatim.

**What this does and does not show.** The clone came over the network from
`github.com/yuitokyouni/sieve` (`main` @ `d8d420a`) into a fresh virtualenv
with `constraints.txt` pins installed from PyPI — a clean *environment*, but
executed on the same host that originally sealed the example. The
*different-machine* evidence is CI: every push reruns the golden path on a
fresh `ubuntu-latest` runner and asserts seal equality with the committed
example (the "seal reproduces on this machine" step), on Python 3.11 and
3.12 — see the Actions tab for the per-run logs.

## Environment

- container Linux x86_64, Python 3.11.15
- numpy 2.4.6, scipy 1.17.1, pydantic 2.13.4, polars 1.43.2
  (installed via `pip install -e . -c constraints.txt`)

## Session log (2026-08-09T02:24:57Z, verbatim)

```
$ git clone https://github.com/yuitokyouni/sieve && cd sieve
HEAD: d8d420aab00aa4aa30bb9dea8c42f673a734d0ae
$ python -m venv .venv && . .venv/bin/activate
$ pip install -e . -c constraints.txt

$ sieve verify docs/example-run
intact: bundle hash and all artifact hashes match

$ sieve test examples/csv_returns --suite financial-daily@1.0 \
      --claim descriptive-market-dynamics --out /tmp/sieve-runs
  PASS         marginal_distribution
  FAIL         tail_behavior
  PASS         return_dependence
  PASS         volatility_dynamics
  FAIL         leverage_asymmetry
  NOT_TESTED   multiscale_behavior
  PASS         drift_nonstationarity
  NOT_TESTED   regime_response
  NOT_TESTED   intervention_validity
  WARN         reproducibility_provenance
findings: 2  bundle bad5cb0455f78d26…

$ sieve verify <run-dir>
intact: bundle hash and all artifact hashes match

$ compare seals
committed example : bad5cb0455f78d2603e1a6767209a11eaacc86e5496d56c9cce4aa9cb87a6490
this fresh venv   : bad5cb0455f78d2603e1a6767209a11eaacc86e5496d56c9cce4aa9cb87a6490
docs/reproduce.md : bad5cb0455f78d2603e1a6767209a11eaacc86e5496d56c9cce4aa9cb87a6490
MATCH

$ pip install -e ".[dev]" -c constraints.txt && python -m pytest tests -q
69 passed, 9 skipped in 39.60s
  (9 skips are the parity layers; enforced instead in the CI parity job)
```

## Verdict

Statuses, both integrity layers, and the byte-exact scientific seal
reproduce from a network clone in a fresh pinned environment. The standalone
test count (69 passed, 9 skipped) matches the documented split; the skipped
parity layers are enforced in CI against a pinned sieve-bench checkout.
